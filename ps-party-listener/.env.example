# Copy to .env and fill in. Never commit .env.
#
# Debug: set DEBUG_PSVOICE=1 for verbose voice/connection logs (rtcBridge, customMessage, ICE, SDP).
#
# Wait for invite: set TEST_WAIT_FOR_INVITE=<online_id> to auto-poll for party invite from that user and join when found.
#
# Party link: set TEST_PARTY_LINK=https://party.playstation.com/-<link_id> to join directly via URL (no polling).
# Get client_id and client_secret from the PlayStation app OAuth flow.
# See the PlayStation Party API research repository or community docs (e.g. PlayStation-Stars API).
# Native app client_id: 09515159-7237-4370-9b40-3806e67c0891

PSN_CLIENT_ID=your_client_id
PSN_CLIENT_SECRET=your_client_secret

# Optional: device id (duid) from your Pixel (or same device you used for captures).
# REQUIRED for "On PS App" / MOBILE_APP: PSN infers platform from OAuth token; Pixel duid + native client_id yields ANDROID_APP token.
# Hex string, 64–128 chars. Example from Pixel 6a (flows 5):
# PSN_DUID=00000007000801a800000000000000533a08957830d9ee3a20202020476f6f676c653a2020506978656c20366100000000000000000000000000000000
# When set, the app also uses Android User-Agent and x-psn-app-ver for m.np requests.

# Optional: override User-Agent for m.np (default: okhttp/4.12.0 when PSN_DUID set, else PlayStationParty)
# PSN_USER_AGENT=okhttp/4.12.0

# Optional: override x-psn-app-ver for m.np (default: PlayStationApp-Android/26.1.0 when PSN_DUID set)
# PSN_APP_VER=PlayStationApp-Android/26.1.0

# Optional: redirect URI. Default is native app scheme (required for token exchange with native client_id).
# Use default (omit) for native client. For localhost callback use: PSN_OAUTH_REDIRECT_URI=http://127.0.0.1:8765/callback
# PSN_OAUTH_REDIRECT_URI=com.scee.psxandroid.scecompcall://redirect
# PSN_CALLBACK_PORT=8765

# Optional: PSAN deviceInfo (from HAR capture). Used when registering device before voice.
# See the research repository for how to extract from your HAR.
# PSAN_DEVICE_TOKEN=
# PSAN_APP_VERSION=26.1.0
# PSAN_TEMPLATE_VERSION=26.01
# PSAN_OS_VERSION=16
# PSAN_LANGUAGE=en-US
# PSAN_DEVICE_MODEL=Pixel 6a
# Set PSAN_DEVICEINFO_MINIMAL=1 to send {} only (if server rejects full body with 500).

# Debug: DEBUG_PSVOICE=1 for verbose voice/customMessage logs (path, body size, payload prefix).
# customMessage: CUSTOM_MSG_TRY_ORDER=v2-no-to to try only channel+payload (no to/withoutSequenceNumber); default tries v2-with-to first.
# HAR replay: REPLAY_HAR_CUSTOMMESSAGE=1 and optionally REPLAY_HAR_CUSTOMMESSAGE_PATH=/path/to/customMessage_204_body.json (default: ./customMessage_204_body.json).
