# Assets

Place **icon.png** here (e.g. 32×32 or 64×64 PNG) for the system tray icon. If missing, the app still runs; the tray icon is simply not shown.

After building, copy this folder to `dist-main/assets` so the packaged app finds the icon, or add a build step that copies it.
